{
    "configurations": [
        {
            "type": "node",
            "request": "attach",
            "port": 9229,
            "address": "localhost",
            "name": "Docker: Attach to Node",
            "localRoot": "${workspaceFolder}/main",
            "remoteRoot": "/app",
            "protocol": "inspector"
        }
        
    ]
}
